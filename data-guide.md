# راهنمای ستون‌های دیتاست — چالش زرین‌پال

## ساختار

هر ردیف نماینده‌ی **یک تلاش پرداخت** است، نه لزوماً یک سشن یکتا.
یک سشن پرداخت می‌تواند چند تلاش داشته باشد؛ در این حالت اطلاعات سطح سشن
(مبلغ، پذیرنده، صنف و…) در هر ردیفِ آن سشن تکرار می‌شود.

فرمت فایل CSV با کدگذاری UTF-8. ستون‌های زمانی به شکل `YYYY-MM-DD HH:MM:SS`.

---

## چرخه‌ی عمر یک پرداخت

```
پذیرنده درخواست پرداخت می‌سازد   →  سشن ایجاد می‌شود
کاربر به درگاه بانک هدایت می‌شود  →  InBank
بانک مبلغ را از کارت برداشت می‌کند →  Paid
پذیرنده تراکنش را verify می‌کند   →  Verified
```

| مقدار | معنا |
|---|---|
| `Verified` | پرداخت انجام شده و پذیرنده آن را تأیید (verify) کرده است — حالت نهایی و کامل |
| `Paid` | مبلغ از کارت خریدار برداشت شده، اما پذیرنده تراکنش را verify نکرده است |
| `InBank` | کاربر به درگاه بانک هدایت شده اما نتیجه‌ای بازنگشته است |
| `Failed` | تلاش/سشن ناموفق بوده است |
| `Reversed` | وجه برگشت خورده است |
| `NoAttempt` | فقط در `try_status`: برای سشنی که هیچ تلاش پرداختی برای آن ثبت نشده |

---

## ستون‌ها

| ستون | نوع | توضیح | خالی می‌شود وقتی |
|---|---|---|---|
| `session_key` | شناسه | شناسه‌ی مستعار سشن پرداخت | — |
| `try_seq` | عدد | شماره‌ی ترتیبی تلاش درون همان سشن، از ۱ به بالا | `0` یعنی سشن هیچ تلاشی نداشته |
| `terminal_key` | شناسه | شناسه‌ی مستعار درگاه پرداخت | — |
| `merchant_key` | شناسه | شناسه‌ی مستعار صاحب حساب. یک پذیرنده می‌تواند چند درگاه داشته باشد | — |
| `category_id` | عدد | کد صنف کسب‌وکار | — |
| `category_title` | متن | عنوان صنف | — |
| `amount` | عدد | مبلغ تراکنش به **ریال** | — |
| `adjusted_fee` | عدد | کارمزد تعدیل‌شده به ریال — یادداشت ۱ | — |
| `session_status` | متن | وضعیت نهایی کل سشن | — |
| `try_status` | متن | وضعیت همین تلاش | — |
| `switch_response_code` | متن | کد پاسخ سوییچ بانکی — یادداشت ۲ | وقتی سوییچ کدی برنگردانده باشد |
| `psp_code` | متن | درگاه پرداختی که **این تلاش** از آن عبور کرده | وقتی تلاشی وجود نداشته باشد |
| `issuer_bank_code` | متن | بانک صادرکننده‌ی کارت خریدار | وقتی اطلاعات کارت بازنگشته باشد — یادداشت ۳ |
| `payer_card_key` | شناسه | شناسه‌ی مستعار کارت خریدار — یادداشت ۴ | همان شرط `issuer_bank_code` |
| `verify_type` | متن | روش verify کردن پذیرنده: `Automated` یا `Manual` | — |
| `init_time_ms` | عدد | مدت زمان فراخوانی API درگاه هنگام شروع پرداخت، به **میلی‌ثانیه** — یادداشت ۵ | وقتی این مرحله طی نشده باشد |
| `verify_time_ms` | عدد | مدت زمان فراخوانی API هنگام verify، به **میلی‌ثانیه** — یادداشت ۵ | وقتی مرحله‌ی verify طی نشده باشد |
| `created_at` | زمان | زمان ایجاد سشن | — |
| `try_created_at` | زمان | زمان ایجاد همین تلاش | وقتی تلاشی وجود نداشته باشد |
| `verified_at` | زمان | زمان verify شدن تراکنش | وقتی تراکنش verify نشده باشد |
| `settled_at` | زمان | زمان تسویه | وقتی تسویه‌ای انجام نشده باشد |
| `expire_in` | زمان | زمان انقضای سشن | — |

---

## یادداشت‌ها

**۱ — `adjusted_fee`.** این مقدار برای حفظ محرمانگی تجاری با یک ضریب ثابت
تعدیل شده و نماینده‌ی تعرفه‌ی واقعی زرین‌پال نیست. چون تعدیل برای تمام
ردیف‌ها یکنواخت است، روابط نسبی — ترتیب، روند زمانی و سهم کارمزد از درآمد —
برای تحلیل معتبر باقی مانده‌اند.

**۲ — `switch_response_code`.** هر درگاه پرداخت فضای کد مخصوص خود را دارد؛
یک عدد یکسان در دو درگاه لزوماً یک معنا ندارد. به همین دلیل مقدار این ستون
به شکل `PSP-xx:code` و مقید به همان درگاه ذخیره شده است. جدول معنی این کدها
در اختیار مسابقه قرار نگرفته است.

**۳ — `issuer_bank_code` و `payer_card_key`.** اطلاعات کارت خریدار تنها زمانی
در اختیار درگاه قرار می‌گیرد که پرداخت در بانک به سرانجام برسد. اگر کاربر
پرداخت را رها کند یا بانک آن را نپذیرد، اطلاعات کارت هرگز بازنمی‌گردد و این
دو ستون خالی می‌مانند.

**۴ — `payer_card_key`.** این شناسه **درون هر پذیرنده** یکتاست. یک کارت
مشخص نزد دو پذیرنده‌ی متفاوت دو شناسه‌ی متفاوت می‌گیرد.

**۵ — ستون‌های زمانِ میلی‌ثانیه‌ای.** این دو، مدت پاسخ‌دهی API درگاه پرداخت
هستند و **زمان تأمل یا تعامل کاربر را نشان نمی‌دهند**.

**۶ — شناسه‌ها.** تمام شناسه‌های سشن، درگاه، پذیرنده، درگاه پرداخت، بانک و
کارت **مستعار** هستند و به هیچ هویت، نام یا شناسه‌ی واقعی اشاره نمی‌کنند.